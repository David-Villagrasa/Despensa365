<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Despensa365"
        tools:targetApi="31">
        <activity
            android:name=".activities.SelectIngrActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.WeekActivity" />
        <activity android:name=".activities.RecipeActivity" />
        <activity android:name=".activities.ToBuyActivity" />
        <activity android:name=".activities.PantryActivity" />
        <activity android:name=".activities.SpecificRecipeActivity" />
        <activity android:name="com.example.despensa365.activities.SelectRecActivity" />
        <activity android:name="com.example.despensa365.activities.RecipeViewActivity" />
    </application>

</manifest>